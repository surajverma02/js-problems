// Write a function that takes a string (a) as argument. Get the first 3 characters of a. Return the result

const str = "Prayagraj";

const firstThreeChar = (str) => str.substring(0, 3);

console.log(firstThreeChar(str))